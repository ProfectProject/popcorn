package com.popcorn.demo.domain.store.controller;

/**
 * TODO: Phase 4 - API 컨트롤러 구현
 * [ ] @RestController, @RequestMapping("/api/stores") 어노테이션 추가
 * [ ] StoreService 의존성 주입
 * [ ] 엔드포인트 구현:
 *     - POST /api/stores (스토어 생성)
 *     - GET /api/stores/my (내 가게 목록)
 *     - GET /api/stores/{storeId} (특정 스토어 조회)
 *     - DELETE /api/stores/{storeId} (스토어 삭제)
 * [ ] @Valid 유효성 검증 적용
 * [ ] @RequestHeader("X-User-Id") 인증 처리
 * [ ] HTTP 상태코드 적절히 설정 (201, 200, 204)
 */
public class StoreController {
}
